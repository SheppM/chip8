{
  "name": "chip8",
  "version": "0.1.0",
  "description": "Just another Chip8 emulator.",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --outfile=public/bundle.js",
    "test": "mocha --check-leaks -r tsconfig-paths/register -r ts-node/register \"test/**/*.ts\""
  },
  "author": "Matthew Sheppard",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.6",
    "esbuild": "^0.15.3",
    "mocha": "^10.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.7.6",
    "tsconfig-paths": "^4.1.0"
  }
}
